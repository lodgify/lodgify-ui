export const DEFAULT_MAX_WIDTH = 12;
export const DEFAULT_COLUMN_WIDTH = 1;

// CSS is computed based on 16 columns by default. 100% means 16
export const SEMANTIC_FULL_WIDTH_COLUMN = 16;

// Quantity of columns to divide Grid and Rows
export const COLUMNS = DEFAULT_MAX_WIDTH;
