patch-package
new file mode 100644
--- /dev/null
+++ b/node_modules/react-styleguidist/lib/utils/getComponent.js
@@ -0,0 +1,21 @@
+/**
+ * Given a component module and a name,
+ * return the appropriate export.
+ *
+ * @param  {object} module
+ * @param  {string} name
+ * @return {function|object}
+ */
+export default function getComponent(module, name) {
+	if (module.default) {
+		return module.default;
+	}
+	if (!module.__esModule && typeof module === 'function') {
+		return module;
+	}
+	const moduleKeys = Object.keys(module);
+	if (moduleKeys.length === 1) {
+		return module[moduleKeys[0]];
+	}
+	return module[name] || module;
+}
--- a/node_modules/react-styleguidist/lib/utils/globalizeComponent.js
+++ b/node_modules/react-styleguidist/lib/utils/globalizeComponent.js
@@ -1,3 +1,5 @@
+import getComponent from './getComponent';
+
 /**
  * Expose component as global variables.
  *
@@ -8,5 +10,5 @@ export default function globalizeComponent(component) {
 		return;
 	}
 
-	global[component.name] = !component.props.path || component.props.path === 'default' ? component.module.default || component.module : component.module[component.props.path];
+	global[component.name] = getComponent(component.module, component.name);
 }
